/compact the conversation

Make sure every single instruction has been completed from the context file or the instructions directly fed to the chat

Add every single feature implemented and bug fixed to the Documentation.txt file ones the context file is done executing. If Changes.md is the context file, remove everything from the file except for "BUGS TO FIX" and "FEATURES TO ADD" and the lines under them, as well as the top line that says "DON'T FORGET ABOUT CLAUDE.md"